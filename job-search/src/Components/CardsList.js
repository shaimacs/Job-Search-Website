import React from 'react';

const CardsList = () => {
    const allJobs = props.jobs.map((job) => {
        return (
            <Card>
                
            </Card>
            )
    })
}

export default CardsList;